image: python:3.10

pipelines:
  tags:
    '*':
      - step:
          name: Deploy
          deployment: Production
          script:
            - pip install build twine
            - python -m build --sdist
            - python -m twine upload dist/*
